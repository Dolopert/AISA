# PROGRESS — AISA Tracker

## สถานะ: P1 เขียนเสร็จ · ยังไม่ได้ทดสอบกับ Supabase จริง

typecheck ผ่าน · `next build` ผ่าน · หน้า login เรนเดอร์จริงบน dev server แล้ว
**`schema.sql` รันบน Supabase จริงแล้ว** (Success, no rows returned) — ข้อกังวลข้อ 1 ตกไป
แต่ flow ล็อกอินและทุก query ยังไม่เคยเดินจริง ดู "ที่ยังไม่ได้พิสูจน์" ด้านล่าง

---

## เสร็จแล้ว (P1)

### ต้นไม้หลักสูตร
- `tools/thai_pdf.py` — ดึงข้อความไทยจาก PDF ตลท. ด้วย PyMuPDF + ซ่อม `ำ` ที่แตก
  (`pdftotext` ใช้ไม่ได้ สลับลำดับสระ/วรรณยุกต์)
- `tools/curriculum.py` — แปลง `LOS_CISA-Foundation.pdf` เป็น `data/curriculum.json`
  ผลลัพธ์ **11 วิชา · 69 บท · 515 LOS** สัดส่วน % รวม 100 พอดี ไม่มีรายการที่ parse พลาด
- สัดส่วนข้อสอบทางการดึงจากตารางหน้า 4 ของคู่มือ (ไม่ได้เดา)

### ฐานข้อมูล
- `supabase/schema.sql` — 12 ตาราง + 2 view + RLS ครบ
- allowlist + trigger ปฏิเสธการสมัครของอีเมลนอกรายชื่อ
- view ใช้ `security_invoker = true` (ถ้าไม่ใส่ RLS จะถูกข้าม แล้วทุกคนอ่านสถิติของคนอื่นได้)

### แอป
- auth **Magic Link** (เปลี่ยนจาก Google ตามที่ผู้ใช้เลือก — Google ต้องตั้ง Google Cloud
  ซึ่งไม่คุ้มเวลาที่เหลือ 10 สัปดาห์) + middleware กันทุกหน้า
- `/auth/callback` รองรับทั้ง `token_hash` (ข้ามเครื่องได้) และ `code` (PKCE เครื่องเดียวกัน)
  เพราะผู้ใช้อ่านเมลบน iPad แต่ขอลิงก์จากคอม
- Dashboard: คะแนนคาดการณ์ถ่วงน้ำหนัก · เกณฑ์ 2 ชั้น · ลำดับวิชาที่ควรซ้อม · ตารางความแม่น + เวลาเทียบงบ
- ทำข้อสอบ 3 โหมด + บันทึกผลจากภายนอก
- ตัวจับเวลา: นับถอยหลัง ไม่มี pause หมดเวลาตัดส่ง (โหมดสนามจริง) และบันทึกเวลา**รายข้อ**
- บันทึกคำตอบทีละข้อทันที ไม่รอจบชุด (เบราว์เซอร์ปิดกลางคันก็ไม่เสียข้อมูลเวลา)
- หน้าเฉลย: แสดง LOS + หนังสืออ้างอิง + ธง "ถูกแต่ช้ากว่างบ"
- หน้าจุดอ่อนระดับ LOS
- ตั้งค่าวันสอบ/เป้าหมาย + โควตารายวันที่คำนวณถอยหลังและปรับเองเมื่อตามไม่ทัน
- ธง `revised_2569` แสดงทุกจุดที่เกี่ยวข้อง

### ตัวตรวจ
- `tools/check-budget.mjs` — จับได้แล้วครั้งหนึ่งว่างบเวลาชุดแรกถ่วงน้ำหนักได้ 93.9 วิ/ข้อ
  ซึ่งเกินเวลาจริง 90 วิ (= ถึงทำได้ตามงบทุกวิชาก็ยังทำไม่ทัน) ปรับใหม่เหลือ 88.82 วิ/ข้อ

---

## ที่ยังไม่ได้พิสูจน์ (ต้องทำก่อนใช้จริง)

1. **ยังไม่ได้ยืนยันว่า view ได้ `security_invoker`** — ถ้า Postgres ไม่รับ option นี้
   RLS จะถูกข้ามและทุกคนอ่านสถิติของคนอื่นได้ ต้องรัน query ตรวจ `pg_class.reloptions`
2. **flow auth ทั้งเส้นยังไม่เคยเดิน** — trigger `handle_new_user` ที่ปฏิเสธอีเมลนอก allowlist
   ยังไม่รู้ว่า Supabase จะโยน error หน้าตาแบบไหน (หน้า login เดาไว้ว่าเป็น `Database error`
   ถ้าไม่ตรงต้องแก้ตัวจับใน `src/app/login/page.tsx`)
3. **ต้องแก้ email template เป็น `{{ .TokenHash }}` เอง** ไม่งั้นกดลิงก์ในเมลบน iPad จะไม่ผ่าน
4. **nested select ของ supabase-js** ในหน้าเฉลยและ `getWeakLos` ถูก cast เป็น `any`
   ถ้าโครงสร้างที่คืนมาไม่ตรงที่คิด จะพังตอน runtime ไม่ใช่ตอน compile
5. **คลังโจทย์ยังว่าง** — ทุกโหมดจะขึ้น "ไม่มีโจทย์ในคลัง" จนกว่าจะ import (นั่นคือ P2)

---

## P2 — คลังโจทย์ (ยังไม่เริ่ม)

- สกัดโจทย์จากไฟล์ตะลุยโจทย์ 12 ไฟล์ใน `AISA/`
  - รูปแบบสม่ำเสมอ: หน้าโจทย์ 4 ตัวเลือก → หน้าเฉลย มีคำอธิบาย + หนังสืออ้างอิง + **วัตถุประสงค์การเรียนรู้**
  - ข้อความ LOS ในหน้าเฉลย**ตรงคำต่อคำ**กับคู่มือ LOS → จับคู่เข้า `los_id` อัตโนมัติได้ ไม่ต้อง tag มือ
  - ประเมินคร่าว ๆ ~250 ข้อ
- **สำรวจแล้วด้วย `tools/survey_drills.py`** — ปัญหา "4 ไฟล์ดึงไม่ออก" แก้จบแล้ว
  ไม่ได้เกิดจากไฟล์เป็นภาพ แต่เพราะหัวเฉลยสะกดต่างกัน 3 แบบ:
  `ข้อ 1 เฉลย ตัวเลือก 1` · `ข้อ 1. เฉลย ตัวเลือก 3)` · `เฉลยตัวเลือกที่ 2` (ไม่มีเลขข้อ)
  และบางไฟล์ใช้ `เอกสารอ้างอิง` แทน `หนังสืออ้างอิง`
  ปรับ regex แล้วสกัดได้ **241 ข้อจาก 11 ไฟล์** (หัก 08-1 ที่ซ้ำกัน 2 ไฟล์ เหลือ ~231 ข้อจริง)
- ไฟล์เดียวที่ใช้ไม่ได้คือ `09-2 ...-Projector` (7 หน้า ว่างทั้งหมด = เป็นภาพ)
  แต่ไม่เสียหาย เพราะเนื้อหาซ้ำกับ `09-1` ที่สกัดได้ครบ 20 ข้อ
- **ยังต้องตรวจ**: ไฟล์ 06 และ 07 นับเฉลยได้ 30 แต่นับ LOS ได้ 27 และ 24
  แปลว่า regex เฉลยอาจจับเกิน หรือบางข้อไม่มีบรรทัด LOS — ต้องดูรายข้อตอนทำ P2 จริง
- นำเข้าเฉลยชุด `AISA-CISA Mock Test - AISA_3rd Set (220 pt).csv`
  **ข้อจำกัด**: CSV มีแต่เฉลย ไม่มี tag วิชารายข้อ → ให้ได้แค่คะแนนรวม
  ต้องมีหน้าให้ระบุช่วงเลขข้อ → วิชา ถึงจะผูกเข้าสถิติรายวิชาได้
- ทำเครื่องหมาย holdout 20%

## P3 — เป้าหมาย/การอ่าน

- เช็กลิสต์ทบทวนรายบท (`reading_progress` มีตารางแล้ว ยังไม่มีหน้า)
- โน้ตย่อผูกกับ LOS (`notes` มีตารางแล้ว ยังไม่มีหน้า)
- โหมดเรียนรายวิชา (อ่านไปซ้อมไป / อ่านจบก่อน) — เก็บใน `user_settings.study_modes` แล้ว ยังไม่มี UI
- diagnostic สั้นในสัปดาห์แรก

## P4 — ขัดเกลา
- deploy Vercel
- หน้า admin จัดการ allowlist

---

## ข้อตกลงที่ห้ามลืม

- **ไม่มี leaderboard** — ตัดออกตามที่ผู้ใช้สั่ง ไม่ใช่ยังไม่ได้ทำ
- **ไม่มี LLM ใน runtime** และ **ไม่ดึงข้อมูลจากเว็บ** — อะไร fix ได้ fix ใน `src/lib/config.ts`
- **ไม่มีปุ่ม pause** ในทุกโหมด
- คลังโจทย์เป็นวงปิดของกลุ่ม New Breed 2026 ที่ซื้อคอร์สเดียวกัน — ไม่เผยแพร่ต่อ ไม่เปิดสมัครเอง
