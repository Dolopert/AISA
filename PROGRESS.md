# PROGRESS — AISA Tracker

## สถานะ: P1 ใช้งานได้จริงบน production · รอเทสต์ล็อกอิน

- deploy แล้วที่ **https://aisa-nu.vercel.app** — middleware เด้ง `/topics` → `/login` ถูกต้อง
  แปลว่า env vars บน Vercel ทำงาน ไม่มี console error
- `schema.sql` รันบน Supabase แล้ว
- **seed หลักสูตรเข้า DB แล้ว: 11 วิชา · 69 บท · 515 LOS · 8 บทติดธงปรับปรุง 1/2569**
  ตรวจจากฐานข้อมูลจริง สัดส่วนรวม 100%
- allowlist มีเจ้าของระบบ 1 รายการ (ลบแถวขยะที่คัดลอก placeholder มาแล้ว)
- repo: https://github.com/Dolopert/AISA (history สะอาด ไม่มีเนื้อหา ตลท. ไม่มีอีเมลจริง)

ยังไม่เคยล็อกอินสำเร็จสักครั้ง ดู "ที่ยังไม่ได้พิสูจน์" ด้านล่าง

---

## เสร็จแล้ว (P1)

### ต้นไม้หลักสูตร
- `tools/thai_pdf.py` — ดึงข้อความไทยจาก PDF ตลท. ด้วย PyMuPDF + ซ่อม `ำ` ที่แตก
  (`pdftotext` ใช้ไม่ได้ สลับลำดับสระ/วรรณยุกต์)
- `tools/curriculum.py` — แปลง `LOS_CISA-Foundation.pdf` เป็น `data/curriculum.json`
  ผลลัพธ์ **11 วิชา · 69 บท · 515 LOS** สัดส่วน % รวม 100 พอดี ไม่มีรายการที่ parse พลาด
- สัดส่วนข้อสอบทางการดึงจากตารางหน้า 4 ของคู่มือ (ไม่ได้เดา)

### ฐานข้อมูล
- `supabase/schema.sql` — 12 ตาราง + 2 view + RLS ครบ
- allowlist + trigger ปฏิเสธการสมัครของอีเมลนอกรายชื่อ
- view ใช้ `security_invoker = true` (ถ้าไม่ใส่ RLS จะถูกข้าม แล้วทุกคนอ่านสถิติของคนอื่นได้)

### แอป
- auth **Magic Link** (เปลี่ยนจาก Google ตามที่ผู้ใช้เลือก — Google ต้องตั้ง Google Cloud
  ซึ่งไม่คุ้มเวลาที่เหลือ 10 สัปดาห์) + middleware กันทุกหน้า
- `/auth/callback` รองรับทั้ง `token_hash` (ข้ามเครื่องได้) และ `code` (PKCE เครื่องเดียวกัน)
  เพราะผู้ใช้อ่านเมลบน iPad แต่ขอลิงก์จากคอม
- Dashboard: คะแนนคาดการณ์ถ่วงน้ำหนัก · เกณฑ์ 2 ชั้น · ลำดับวิชาที่ควรซ้อม · ตารางความแม่น + เวลาเทียบงบ
- ทำข้อสอบ 3 โหมด + บันทึกผลจากภายนอก
- ตัวจับเวลา: นับถอยหลัง ไม่มี pause หมดเวลาตัดส่ง (โหมดสนามจริง) และบันทึกเวลา**รายข้อ**
- บันทึกคำตอบทีละข้อทันที ไม่รอจบชุด (เบราว์เซอร์ปิดกลางคันก็ไม่เสียข้อมูลเวลา)
- หน้าเฉลย: แสดง LOS + หนังสืออ้างอิง + ธง "ถูกแต่ช้ากว่างบ"
- หน้าจุดอ่อนระดับ LOS
- ตั้งค่าวันสอบ/เป้าหมาย + โควตารายวันที่คำนวณถอยหลังและปรับเองเมื่อตามไม่ทัน
- ธง `revised_2569` แสดงทุกจุดที่เกี่ยวข้อง

### ขอบเขต repo
- **โค้ดกับเนื้อหาข้อสอบแยกขาดจากกัน** — repo เก็บเครื่องมืออย่างเดียว
  คลังโจทย์นำเข้า Supabase จากเครื่องโดยตรง ไม่ผ่าน git
  ทำให้ repo ปลอดภัยไม่ว่าจะตั้ง public หรือ private
- ระหว่างคลังว่าง หน้า "ทำข้อสอบ" ขึ้น **Coming Soon** แทน error
  และ "บันทึกผลจากภายนอก" ยังใช้ได้ตามปกติ

### ตัวตรวจ
- `tools/check-budget.mjs` — จับได้แล้วครั้งหนึ่งว่างบเวลาชุดแรกถ่วงน้ำหนักได้ 93.9 วิ/ข้อ
  ซึ่งเกินเวลาจริง 90 วิ (= ถึงทำได้ตามงบทุกวิชาก็ยังทำไม่ทัน) ปรับใหม่เหลือ 88.82 วิ/ข้อ
- `tools/check-no-content.mjs` — fail ถ้ามี PDF/XLSX ถูก track หรือไฟล์ข้อมูลบรรจุเฉลย
- `npm run check` รวมทั้งหมด — **รันก่อน push ทุกครั้ง**

---

## ที่ยังไม่ได้พิสูจน์ (ต้องทำก่อนใช้จริง)

1. **ต้องรัน `supabase/patch-01-lock-views-to-caller.sql`** — view เดิมพึ่ง `security_invoker`
   อย่างเดียว ซึ่งยังไม่เคยยืนยันว่าติดจริง patch นี้เพิ่มการกรอง `auth.uid()`
   ลงในตัว view ทำให้ปลอดภัยแม้ option จะไม่ติด **ยังไม่ได้รัน**
2. **flow auth ทั้งเส้นยังไม่เคยเดิน** — trigger `handle_new_user` ที่ปฏิเสธอีเมลนอก allowlist
   ยังไม่รู้ว่า Supabase จะโยน error หน้าตาแบบไหน (หน้า login เดาไว้ว่าเป็น `Database error`
   ถ้าไม่ตรงต้องแก้ตัวจับใน `src/app/login/page.tsx`)
3. **แพลนฟรีแก้ email template ไม่ได้** (ต้องต่อ custom SMTP ก่อน) จึงยังใช้ PKCE
   → **ต้องขอลิงก์และเปิดเมลบนเครื่องเดียวกัน** ข้ามเครื่องไม่ได้
   ถ้าเริ่มรำคาญให้ต่อ SMTP ฟรีแล้วแก้ template เป็น `{{ .TokenHash }}`
   (`/auth/callback` รองรับอยู่แล้ว ไม่ต้องแก้โค้ด)
4. **nested select ของ supabase-js** ในหน้าเฉลยและ `getWeakLos` ถูก cast เป็น `any`
   ถ้าโครงสร้างที่คืนมาไม่ตรงที่คิด จะพังตอน runtime ไม่ใช่ตอน compile
5. **คลังโจทย์ยังว่าง** — ทุกโหมดจะขึ้น "ไม่มีโจทย์ในคลัง" จนกว่าจะ import (นั่นคือ P2)

---

## P2 — คลังโจทย์ (ยังไม่เริ่ม · แยกออกมาโดยตั้งใจ)

> เลื่อนออกไปตามที่ผู้ใช้ตัดสินใจ เพราะโจทย์ยังต้องเกลา และการแยกทำให้ repo
> ไม่มีเนื้อหาลิขสิทธิ์ ปลอดภัยพอที่จะตั้ง public ได้
> **ผลลัพธ์ของ P2 ต้องไม่ commit เข้า repo** — seed เข้า Supabase จากเครื่องเท่านั้น


- สกัดโจทย์จากไฟล์ตะลุยโจทย์ 12 ไฟล์ใน `AISA/`
  - รูปแบบสม่ำเสมอ: หน้าโจทย์ 4 ตัวเลือก → หน้าเฉลย มีคำอธิบาย + หนังสืออ้างอิง + **วัตถุประสงค์การเรียนรู้**
  - ข้อความ LOS ในหน้าเฉลย**ตรงคำต่อคำ**กับคู่มือ LOS → จับคู่เข้า `los_id` อัตโนมัติได้ ไม่ต้อง tag มือ
  - ประเมินคร่าว ๆ ~250 ข้อ
- **สำรวจแล้วด้วย `tools/survey_drills.py`** — ปัญหา "4 ไฟล์ดึงไม่ออก" แก้จบแล้ว
  ไม่ได้เกิดจากไฟล์เป็นภาพ แต่เพราะหัวเฉลยสะกดต่างกัน 3 แบบ:
  `ข้อ 1 เฉลย ตัวเลือก 1` · `ข้อ 1. เฉลย ตัวเลือก 3)` · `เฉลยตัวเลือกที่ 2` (ไม่มีเลขข้อ)
  และบางไฟล์ใช้ `เอกสารอ้างอิง` แทน `หนังสืออ้างอิง`
  ปรับ regex แล้วสกัดได้ **241 ข้อจาก 11 ไฟล์** (หัก 08-1 ที่ซ้ำกัน 2 ไฟล์ เหลือ ~231 ข้อจริง)
- ไฟล์เดียวที่ใช้ไม่ได้คือ `09-2 ...-Projector` (7 หน้า ว่างทั้งหมด = เป็นภาพ)
  แต่ไม่เสียหาย เพราะเนื้อหาซ้ำกับ `09-1` ที่สกัดได้ครบ 20 ข้อ
- **ยังต้องตรวจ**: ไฟล์ 06 และ 07 นับเฉลยได้ 30 แต่นับ LOS ได้ 27 และ 24
  แปลว่า regex เฉลยอาจจับเกิน หรือบางข้อไม่มีบรรทัด LOS — ต้องดูรายข้อตอนทำ P2 จริง
- นำเข้าเฉลยชุด `AISA-CISA Mock Test - AISA_3rd Set (220 pt).csv`
  **ข้อจำกัด**: CSV มีแต่เฉลย ไม่มี tag วิชารายข้อ → ให้ได้แค่คะแนนรวม
  ต้องมีหน้าให้ระบุช่วงเลขข้อ → วิชา ถึงจะผูกเข้าสถิติรายวิชาได้
- ทำเครื่องหมาย holdout 20%

## P3 — เป้าหมาย/การอ่าน

- เช็กลิสต์ทบทวนรายบท (`reading_progress` มีตารางแล้ว ยังไม่มีหน้า)
- โน้ตย่อผูกกับ LOS (`notes` มีตารางแล้ว ยังไม่มีหน้า)
- โหมดเรียนรายวิชา (อ่านไปซ้อมไป / อ่านจบก่อน) — เก็บใน `user_settings.study_modes` แล้ว ยังไม่มี UI
- diagnostic สั้นในสัปดาห์แรก

## P4 — ขัดเกลา
- deploy Vercel
- หน้า admin จัดการ allowlist

---

## ข้อตกลงที่ห้ามลืม

- **ไม่มี leaderboard** — ตัดออกตามที่ผู้ใช้สั่ง ไม่ใช่ยังไม่ได้ทำ
- **ไม่มี LLM ใน runtime** และ **ไม่ดึงข้อมูลจากเว็บ** — อะไร fix ได้ fix ใน `src/lib/config.ts`
- **ไม่มีปุ่ม pause** ในทุกโหมด
- คลังโจทย์เป็นวงปิดของกลุ่ม New Breed 2026 ที่ซื้อคอร์สเดียวกัน — ไม่เผยแพร่ต่อ ไม่เปิดสมัครเอง

---

## เปลี่ยนแกนหลัก (หลังผู้ใช้ทดลองใช้)

ผู้ใช้ระบุว่าสิ่งที่ต้องการจริงคือ **Tracking การอ่าน** ไม่ใช่การตะลุยโจทย์
ซึ่งขัดกับที่ตกลงไว้ตอน Q2/Q17 — ปรับตามความต้องการจริง

**ไม่ได้รื้อใหม่** เพราะของเดิมส่วนใหญ่รองรับอยู่แล้ว:
ต้นไม้หลักสูตร 69 บท/515 LOS คือรายการที่ต้องอ่านพอดี · `reading_progress`
กับ `notes` มีในสคีมาตั้งแต่แรก · auth/RLS/วันสอบ/ธง revised_2569 ใช้ได้ทั้งหมด
สิ่งที่เปลี่ยนคือ **หน้าแรกวัดอะไร** ไม่ใช่โครงสร้างข้างล่าง

### ทำแล้ว
- `STUDY_MINUTES_PER_LOS` ประมาณเวลาศึกษาต่อ LOS แยกตามวิชา
  เวลาของบท = จำนวน LOS × ค่าของวิชานั้น
- `tools/check-study-plan.mjs` ตรวจว่าแผนพอดีกับเวลาที่เหลือไหม
  **ผลตอนนี้: ทั้งหลักสูตร ~173.8 ชม. ที่ 15 ชม./สัปดาห์ × 10 สัปดาห์ = ขาด 23.8 ชม.**
  ต้องอ่านจริง ~2.5 ชม./วัน — ไม่ได้ลดค่าประมาณลงเพื่อให้ตัวเลขดูผ่าน
- `patch-02-reading-sessions.sql` ตาราง `reading_sessions` + view
  `chapter_minutes_spent` และ `daily_reading`
- หน้า `/read` — ต้นไม้วิชา → บท พร้อมติ๊กจบบท ดูหัวข้อย่อย (LOS)
  เวลาประมาณการ เวลาที่ใช้จริง และป้ายเตือนบทที่ถูกแก้ 1/2569
- ตัวจับเวลาอ่านรายบท (เริ่ม/หยุด) + กรอกย้อนหลัง เก็บสถานะใน localStorage
  **การอ่านหยุดพักได้** — กฎห้าม pause ใช้กับการทำข้อสอบเท่านั้น
- หน้าแรกเปลี่ยนตัวเลขหลักเป็น % การอ่าน (ถ่วงน้ำหนักด้วยเวลา ไม่ใช่นับบท)
  ผลข้อสอบลดชั้นลงไปเป็นการ์ดรอง
- เตือนเมื่อความเร็วจริงช้ากว่าที่ประมาณเกิน 15%

### ยังไม่ได้ทำ
- **ยังไม่ได้รัน `patch-02` บน Supabase** → `/read` และหน้าแรกจะพังจนกว่าจะรัน
- โน้ตย่อผูก LOS (ตาราง `notes` มีแล้ว ยังไม่มีหน้า)
- ยังไม่ได้ทดสอบ `/read` กับข้อมูลจริงสักครั้ง

---

## การเข้าสู่ระบบ — ตัดสินใจแล้ว (ทางเลือก C)

ผู้ใช้เลือก **คงการเข้าระบบด้วยอีเมล + รหัสผ่านไว้** หลังพิจารณา 3 ทางเลือก
(Google OAuth / ลิงก์ทางเมลอย่างเดียว / รหัสผ่าน)

**สิ่งที่ผู้ใช้รับทราบและยอมรับแล้ว:**
- โปรเจกต์ Supabase นี้จะเก็บ bcrypt hash รหัสผ่านของเพื่อนไว้
  แอปเราไม่เคยเห็นและไม่เก็บรหัสผ่าน แต่เจ้าของโปรเจกต์เป็นผู้ดูแลข้อมูลนั้น
- แพลนฟรีไม่มี SMTP ของตัวเอง **การรีเซ็ตรหัสผ่านที่ลืมจึงเป็นภาระของเจ้าของระบบ**
  ต้องรีเซ็ตให้จาก Supabase dashboard หรือรัน `tools/create-admin.mjs`
- คนที่ใช้รหัสผ่านซ้ำกับเว็บอื่น จะได้รับผลกระทบเกินขอบเขตแอปนี้ถ้าฐานข้อมูลรั่ว

**ทางเข้าที่มีตอนนี้:**
1. อีเมล + รหัสผ่าน (ค่าเริ่มต้น) — พิมพ์ชื่อสั้นได้ ระบบเติม `@aisa-tracker.local` ให้
2. ลิงก์ทางเมล — ทางสำรองสำหรับคนที่ยังไม่เคยตั้งรหัสผ่าน และใช้กู้คืนเมื่อลืมรหัส
3. บัญชีผู้ดูแล `admin@aisa-tracker.local` สร้างด้วย `tools/create-admin.mjs`
   รหัสสุ่ม 20 ตัว เก็บใน `.admin-credentials.local` ที่ gitignore ไว้

**ถ้าจะเปลี่ยนใจภายหลัง** — Google OAuth ยังเป็นทางที่ไม่ต้องถือรหัสผ่านใครเลย
ต้นทุนคือตั้ง Google Cloud ครั้งเดียว ~15 นาที
