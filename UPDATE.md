# UPDATE — สรุปสถานะสำหรับ session ถัดไป

อ่านไฟล์นี้ก่อน แล้วค่อยดู `README.md` (วิธีติดตั้ง) และ `PROGRESS.md` (ประวัติการตัดสินใจ)

**อัปเดตล่าสุด:** 1 ก.ย. 2569 · commit `049ff81`

---

## นี่คืออะไร

เว็บแอปติดตามความพร้อมสอบ **AISA** (CISA ระดับ Foundation ของตลาดหลักทรัพย์ฯ)
แกนหลักคือ **ติดตามการอ่าน** — วัดเป็น % ที่ถ่วงน้ำหนักด้วยเวลาที่แต่ละบทต้องใช้
ส่วนการทำข้อสอบเป็นตัวรองไว้ยืนยันว่าที่อ่านไปเข้าหัวจริง

| | |
|---|---|
| Production | https://aisa-nu.vercel.app |
| Repo | https://github.com/Dolopert/AISA (**public**) |
| Supabase | project `ueyxgycocdwlnmlbpqoe` |
| วันสอบ | **อา. 22 พ.ย. 2569** · รับสมัคร 7 ก.ย. – 8 พ.ย. 2569 |

> `aisa.vercel.app` (ไม่มี `-nu`) **เป็นเว็บของคนอื่น** ชื่อถูกจองไปแล้ว
> เปิดผิดโดเมนจะเจอ 404 — เคยหลงมาแล้วหนึ่งรอบ

---

## ใช้งานได้จริงแล้ว

- deploy บน Vercel · env ถูกต้อง (ยืนยันจาก bundle ไม่ใช่จากหน้าจอ)
- เข้าระบบด้วย **อีเมล + รหัสผ่าน** (พิมพ์ชื่อสั้นได้ ระบบเติม `@aisa-tracker.local`) หรือ **ลิงก์ทางเมล**
- allowlist กันคนนอกได้จริง — ทดสอบด้วยอีเมลสุ่มแล้วถูกปฏิเสธที่ระดับ DB
- RLS ปิดข้อมูลรายบุคคล view กรอง `auth.uid()` ในตัวเอง ไม่พึ่ง `security_invoker` อย่างเดียว
- หลักสูตรอยู่ใน DB แล้ว: **11 วิชา · 69 บท · 515 LOS** สัดส่วนรวม 100%
- ธีมสว่าง/มืด (ดำน้ำเงิน) · mobile-first
- บัญชีผู้ดูแล `admin@aisa-tracker.local` รหัสอยู่ใน `.admin-credentials.local` (gitignore ไว้)

## ยังไม่เคยพิสูจน์

1. **หน้า `/read` ไม่เคยถูกใช้กับข้อมูลจริงสักครั้ง** — ติ๊กบท จับเวลา กรอกย้อนหลัง
   ยังไม่มีใครกดจริง ปฏิทินจึงยังไม่เคยแสดงข้อมูล
2. **RLS แยกผู้ใช้ยังพิสูจน์ไม่ได้** เพราะมีผู้ใช้จริงคนเดียว ต้องมีผู้ใช้ที่ 2 ถึงจะยืนยันได้
3. **คลังโจทย์ว่าง** — หน้าทำข้อสอบขึ้น "Coming Soon" ตามที่ออกแบบไว้

---

## ข้อตกลงที่ห้ามรื้อ (ผ่านการถาม-ตอบมาแล้ว อย่าเสนอใหม่)

| | |
|---|---|
| **ไม่มี leaderboard** | ตัดออกตามที่สั่ง ไม่ใช่ยังไม่ได้ทำ |
| **ไม่มี LLM ตอน runtime** | สกัด PDF ทำตอน dev ครั้งเดียว ไม่มี API key ในระบบ |
| **ไม่ดึงข้อมูลจากเว็บ** | อะไร fix ได้ fix ใน `src/lib/config.ts` |
| **ไม่มีปุ่ม pause ตอนทำข้อสอบ** | แต่**การอ่านหยุดพักได้** — คนละเรื่องกัน |
| **เข้าระบบด้วยรหัสผ่าน** | เลือกแล้ว (ทางเลือก C) รับทราบเรื่องถือ bcrypt hash ของเพื่อนและภาระรีเซ็ตแล้ว |
| **โค้ดกับเนื้อหาข้อสอบแยกขาดกัน** | repo เป็น public — โจทย์/เฉลยของ ตลท. ห้ามเข้า repo เด็ดขาด |

---

## ตัวเลขที่ควรรู้

- **แผนการอ่านทั้งหลักสูตร 173.8 ชม.** — ที่ 15 ชม./สัปดาห์ × 11 สัปดาห์ **ยังขาด 8.8 ชม.**
  หนักสุด: งบการเงิน 40.3 · ตราสารทุน 37.3 · อนุพันธ์ 27.5
- งบเวลาทำข้อสอบถ่วงน้ำหนักแล้ว **88.82 วิ/ข้อ** จากที่มีจริง 90 วิ (เหลือกันชน 1.18)
- ไฟล์ตะลุยโจทย์สกัดเฉลยได้ **241 ข้อจาก 11 ไฟล์** (หักไฟล์ซ้ำเหลือ ~231 ข้อจริง)
- เกณฑ์ผ่าน **2 ชั้น**: รวม ≥70% **และ** กลุ่มวิชาที่ 1 (จรรยาบรรณ) ≥70%

---

## งานถัดไป

### P2 — คลังโจทย์ (ยังไม่เริ่ม)
สำรวจเสร็จแล้วด้วย `tools/survey_drills.py` รู้แล้วว่าสกัดได้ ~231 ข้อ
- หัวเฉลยสะกดต่างกัน **3 แบบ**: `ข้อ 1 เฉลย ตัวเลือก 1` · `ข้อ 1. เฉลย ตัวเลือก 3)` · `เฉลยตัวเลือกที่ 2`
- บางไฟล์ใช้ `เอกสารอ้างอิง` แทน `หนังสืออ้างอิง`
- **ข้อความ LOS ในหน้าเฉลยตรงคำต่อคำกับคู่มือ LOS** → จับคู่เข้า `los_id` อัตโนมัติได้
- ไฟล์ `09-2 Projector` เป็นภาพล้วน ใช้ไม่ได้ แต่ซ้ำกับ `09-1` ที่สกัดได้ครบ
- ต้องตรวจ: ไฟล์ 06/07 นับเฉลยได้ 30 แต่ LOS ได้ 27/24 — regex อาจจับเกิน
- **ผลลัพธ์ห้าม commit** seed เข้า Supabase จากเครื่องเท่านั้น

### P3 — ที่เหลือ
- โน้ตย่อผูก LOS (ตาราง `notes` มีแล้ว ยังไม่มีหน้า)
- นำเข้าเฉลย mock CSV 220 คะแนน (ต้องมีหน้าระบุช่วงข้อ → วิชา ก่อนถึงผูกสถิติรายวิชาได้)
- ทำเครื่องหมาย holdout 20%

---

## คำสั่ง

```bash
npm run dev
```

```bash
npm run check
```

`check` = typecheck + งบเวลา + เทสต์ตรรกะเตือน (12 ข้อ) + กันเนื้อหาลิขสิทธิ์ — **รันก่อน push ทุกครั้ง**

| เครื่องมือ | ทำอะไร |
|---|---|
| `node tools/check-deploy.mjs` | production เสิร์ฟ build ไหน env ถูกฝังเป็นค่าอะไร |
| `node tools/smoke-auth.mjs` | ทดสอบ auth + RLS กับของจริงโดยไม่ต้องแตะกล่องเมล |
| `node tools/seed.mjs` | นำเข้าหลักสูตรเข้า Supabase |
| `node tools/create-admin.mjs [ชื่อ]` | สร้าง/ตั้งรหัสใหม่ให้บัญชีผู้ดูแล |
| `python tools/curriculum.py <LOS.pdf> data/curriculum.json` | สร้างต้นไม้หลักสูตร |
| `python tools/survey_drills.py <โฟลเดอร์ AISA>` | สำรวจว่าไฟล์ตะลุยโจทย์สกัดได้แค่ไหน |

---

## กับดักที่เจอมาแล้ว — อย่าเหยียบซ้ำ

**1. `next build` ทับ `.next` ของ dev server** ทำ dev พังทุกหน้าด้วย `Cannot find module './611.js'`
ทั้งที่โค้ดไม่ผิด → ใช้ `NEXT_DIST_DIR=.next-verify` เสมอเวลา build ตรวจสอบ

**2. `NEXT_PUBLIC_*` ถูกฝังตอน build ไม่ใช่ตอนรัน** แก้ค่าบน Vercel แล้ว**ต้อง redeploy**
และอย่าเชื่อว่า "middleware redirect ได้ = env ถูก" — redirect เกิดได้แม้ต่อ Supabase ไม่ติด
ให้ตรวจที่ bundle ด้วย `check-deploy.mjs` เท่านั้น

**3. `pdftotext` ใช้กับ PDF ไทยของ ตลท. ไม่ได้** สลับลำดับสระ/วรรณยุกต์ (`ข้อ` → `ขอ้`)
ต้องใช้ PyMuPDF ผ่าน `tools/thai_pdf.py` ซึ่งซ่อม `ำ` ที่แตกเป็น `<พยัญชนะ> า` ให้ด้วย

**4. Tailwind ใส่ opacity modifier บน `var()` ไม่ได้** — `border-[var(--color-x)]/30`
ไม่เคยแสดงผลเลยตั้งแต่แรก เพิ่งเห็นตอนทำ dark mode ต้องใช้ token เส้นขอบแยก

**5. PowerShell `Set-Content -Encoding utf8` ใส่ BOM** ทำ `package.json` พังจน build ไม่ขึ้น
เขียนไฟล์ที่เครื่องมืออื่นต้องอ่านด้วย Python/Write แทน

**6. Bash tool ในเครื่องนี้ PATH เพี้ยนเป็นช่วง ๆ** (`grep`, `head`, `node`, `python` หายไปเฉย ๆ)
ถ้าเจอ `command not found` แปลก ๆ ให้ย้ายไปใช้ PowerShell

**7. สคริปต์เขียน allowlist ก่อนตรวจ input** เคยทิ้งแถวขยะไว้ 2 ครั้ง (`อีเมลของคุณ`, `admin`)
แก้แล้วให้ตรวจรูปแบบก่อนแตะฐานข้อมูล — ระวังรูปแบบนี้ในสคริปต์ใหม่

**8. Supabase บังคับ identifier เป็นรูปแบบอีเมล** พิมพ์ `admin` เฉย ๆ ไม่ผ่าน
แก้ที่ชั้น UI ด้วย `toLoginEmail()` ไม่ใช่ที่ฐานข้อมูล

---

## ข้อจำกัดที่ยังอยู่

- **แพลนฟรีแก้ email template ไม่ได้** (ต้องต่อ custom SMTP ก่อน) ลิงก์ทางเมลจึงยังเป็น PKCE
  → **ต้องขอลิงก์และเปิดเมลบนเบราว์เซอร์เดียวกัน** ข้ามเครื่องไม่ได้
  ถ้าต่อ SMTP แล้ว ให้แก้ template เป็น `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=magiclink`
  — `/auth/callback` รองรับอยู่แล้ว ไม่ต้องแก้โค้ด
- SMTP ในตัวจำกัดราว **2-4 อีเมล/ชั่วโมงทั้งโปรเจกต์**
- **การรีเซ็ตรหัสผ่านที่ลืมเป็นภาระของเจ้าของระบบ** ต้องทำจาก dashboard เอง
- `data/curriculum.json` ไม่อยู่ใน repo (เป็นข้อความจากคู่มือ ตลท.) ต้องสร้างเองก่อน seed
